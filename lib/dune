(library
 (name lib)
 (public_name micse.lib)
 (libraries core utils yojson zarith sexplib bignum)
 (preprocess
  (pps ppx_sexp_conv ppx_compare)))

(menhir
 (modules michParser)
 (flags --explain))

(ocamllex michLexer)
